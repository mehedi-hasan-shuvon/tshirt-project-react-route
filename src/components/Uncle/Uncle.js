import React from 'react';

const Uncle = ({house}) => {
    return (
        <div>
            <h2>This is uncle</h2>
            <p>house: {house}</p>
        </div>
    );
};

export default Uncle;